import { Box, BoxProps } from "@chakra-ui/react";
import { FC } from "react";

const DEFAULT_BOX_SHADOW = "";

export const StandardConcaveBox: FC<BoxProps> = (props) => {
    const boxShadow = "inset 2px -4px 8px";

    return <Box>

    </Box>
}